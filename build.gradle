buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        // version conflict not reported with 1.3.1
//        classpath 'org.springframework.boot:spring-boot-gradle-plugin:1.2.8.RELEASE'
        classpath 'org.springframework.boot:spring-boot-gradle-plugin:1.3.1.RELEASE'
    }
}

repositories {
    jcenter()
}

apply plugin: 'java'
apply plugin: 'spring-boot'

configurations.all {
    resolutionStrategy {
        failOnVersionConflict()
    }
}

dependencies {
    compile 'org.jetbrains:annotations:13.0', 'org.jetbrains:annotations:15.0'
}

wrapper {
    gradleVersion = '2.10'
}
